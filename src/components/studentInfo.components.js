import React from "react";
import { View, Text, StyleSheet, Dimensions } from 'react-native';

const windowWidth = Dimensions.get('window').width;

export const SutentInfo = () => {
  return (
    <Text>Информация об обучении</Text>
  )
}
